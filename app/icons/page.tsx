import IconsClient from './icons-client'

export const metadata = {
  title: 'Browse icons — ghicons',
  description:
    'Browse all 106 Ghanaian cultural symbols. Filter by category, preview at any size and colour, and copy the code or the raw SVG.',
}

export default function IconsPage() {
  return <IconsClient />
}
